name: narda_core
description: >-
  Pure Dart core for Uzun Narda (long backgammon): immutable game state,
  rules, maximal move-sequence generator and heuristic bot.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
